<?xml version="1.0" encoding="UTF-8"?>
<Server port="8005" shutdown="SHUTDOWN">
 
    <Listener className="org.apache.catalina.startup.VersionLoggerListener" />
    <Listener className="org.apache.catalina.core.AprLifecycleListener" SSLEngine="on" />
    <Listener className="org.apache.catalina.core.JreMemoryLeakPreventionListener" />
    <Listener className="org.apache.catalina.mbeans.GlobalResourcesLifecycleListener" />
    <Listener className="org.apache.catalina.core.ThreadLocalLeakPreventionListener" />
 
    <GlobalNamingResources>
        <Resource name="UserDatabase"
                  auth="Container"
                  type="org.apache.catalina.UserDatabase"
                  description="User database that can be updated and saved"
                  factory="org.apache.catalina.users.MemoryUserDatabaseFactory"
                  pathname="conf/tomcat-users.xml" />
    </GlobalNamingResources>
 
    <Service name="Catalina">
 
        <!-- ============================================================
             Connector
             ・port        : HTTPリクエストを受け付けるポート
             ・maxThreads  : 同時に処理できるリクエスト数の上限
                             開発用途なので 50 に絞る（デフォルトは 200）
             ・minSpareThreads : 常時待機させるスレッド数
             ・connectionTimeout : 接続タイムアウト（ミリ秒）
             ============================================================ -->
        <Connector port="8080"
                   protocol="HTTP/1.1"
                   maxThreads="50"
                   minSpareThreads="5"
                   connectionTimeout="20000"
                   redirectPort="8443"
                   URIEncoding="UTF-8" />
 
        <!-- AJP は使用しないので無効化 -->
        <!-- <Connector protocol="AJP/1.3" ... /> -->
 
        <Engine name="Catalina" defaultHost="localhost">
 
            <Realm className="org.apache.catalina.realm.LockOutRealm">
                <Realm className="org.apache.catalina.realm.UserDatabaseRealm"
                       resourceName="UserDatabase" />
            </Realm>
 
            <Host name="localhost" appBase="webapps"
                  unpackWARs="true" autoDeploy="true">
 
                <!-- ============================================================
                     Context
                     ・path     : コンテキストパス（アクセスURL のベース）
                                  http://localhost:8080/echo-server/...
                     ・docBase  : WAR ファイル名（webapps/ からの相対パス）
                     ・reloadable : クラス変更を自動検知して再読込するか
                                   開発中は true が便利だが、本番では false にする
                     ============================================================ -->
                <Context path="/echo-server"
                         docBase="echo-server"
                         reloadable="true" />
 
                <!-- ============================================================
                     AccessLogValve
                     ・directory : ログ出力先ディレクトリ
                     ・prefix    : ログファイル名のプレフィックス
                     ・suffix    : ログファイル名のサフィックス
                     ・pattern   : ログのフォーマット
                                   %h=クライアントIP %t=日時 %r=リクエスト行
                                   %s=ステータスコード %b=レスポンスサイズ(byte)
                                   %D=処理時間(ミリ秒)
                     ============================================================ -->
                <Valve className="org.apache.catalina.valves.AccessLogValve"
                       directory="logs"
                       prefix="access_log"
                       suffix=".txt"
                       pattern="%h %l %u %t &quot;%r&quot; %s %b %D"
                       rotatable="true" />
 
            </Host>
        </Engine>
    </Service>
</Server>